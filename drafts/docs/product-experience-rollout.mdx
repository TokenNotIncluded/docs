---
title: "用户体验与来源分析实施清单"
sidebarTitle: "体验实施清单"
description: "用户体验与来源分析的实施清单与发布顺序。"
icon: "list-checks"
---

当前未完成事项统一跟踪：[GitHub #380](https://github.com/TokenNotIncluded/api.lmm.best/issues/380)。下方批次为历史实施记录，早期待办状态已被后续实现部分替代；以 issue 中的剩余实现与验收清单为准。

范围：2026-09-19 用户提出的全部 27 项，已授权分批持续实施。以下状态只表示实际完成情况，设计或代码存在不等于验证通过。保留任务开始前已有的工作区修改；提交、推送、生产发布尚未执行。

## 批次与验收

| 批次 | 原始条目 | 工作与验收条件 | 状态 |
| --- | --- | --- | --- |
| 账号与引导 | 1、2、3、8、9、16、17、20、21、27 | OAuth/Key 分流；统一真实账号状态和下一步；指南延续设备与客户端；审批详情与重新提交；首页收敛 | 进行中 |
| 模型与计费 | 4、5、11、13 | 价格入口承诺一致；费用估算含适用规则；权限与运行状态分开；调用费用与退款有真实依据 | 待实施 |
| 导航与术语 | 6、7 | 七语言名称一致；使用 AI、开发者、生态、账户分区；管理员单独导航 | 待实施 |
| 密钥与排错 | 10、12、22、23、24 | 简洁创建、分组切换联动；敏感信息过滤；错误恢复；移动端与空状态 | 待实施 |
| 助手 | 14、15 | 按账号状态执行引导；写操作确认；会话连续、重试幂等、历史重新加载 | 待实施 |
| 赏金 | 18、19 | 决策信息与真实流程时间线；奖励明确为 API 账户余额 | 待实施 |
| 强制公告 | 25 | 管理员开关；按发布时间依次阅读；进度与确认；账号及内容版本级去重；管理查询 | 已实现并完成本地定向验证，待真实部署验收 |
| 来源采集 | 26.1—26.5、26.11 | 最少采集与许可；清洗 URL；稳定推广标识、测试排除；来源证据独立；首次/注册/付费/最近归属；可靠游客关联；用户自述独立保存；保留未知 | 基础采集、注册归属、推广链接已实现；其余待实施 |
| 来源报表 | 26.6—26.10 | 账号去重、成功调用、实付/退款/币种分离；同批漏斗与留存；时间线、权限与导出；成本对照；覆盖率与数据缺失；幂等、恢复、清理、不阻断业务 | 已有注册渠道及现金付款汇总；完整转化与留存待实施 |

## 实现边界

- API 访问以服务端授权为准；OAuth 不等于绕过访问审批，OAuth 授权不等于成功调用。
- 引导响应新增 `api_key_created`，区分手动 Key 与 OAuth 管理凭据；`details_available` 区分查询失败与确实没有记录。
- 未获取到数据必须显示未知/加载失败，不视为未申请、未调用或零收入。
- 公告阅读确认不是分析追踪同意。滚动到底只能证明界面操作，不能证明用户理解了内容。
- 来源归属不修改邀请和财务记录；历史账号不可伪造首次来源；跨设备仅依赖可靠账号关联。
- 不新增硬编码的模型运行状态、退款状态或价格。没有事实来源时明确说明缺失。

## 验证记录

- 首页主要操作从购买改为账号下一步；OAuth 与 Key 步骤可切换；首页取消“开始前比较价格”的不实承诺，暂不改变价格访问策略。
- Dashboard 与真实的新手助手布局接入账号状态区，审批详情包含时间、申请正文、AI 建议和管理员备注。旧 Getting Started 组件同样更新，但实际布局会接管该路由。
- 客户端工具在 sessionStorage 中仅保留设备和客户端选项，不保存凭证或完成状态。新预设、两条路线的完整进度共享仍待后续完成。
- 新增 `AnnouncementRead` 迁移；公告按发布时间升序、同时间按 ID 排序。确认按账号、公告 ID、内容摘要去重，正文变更后重新确认；未来公告暂不进入队列。管理员可在用户编辑页查看当前公告阅读时间。
- 浏览器公告门禁控制控制台界面；确认接口校验队列顺序。滚动进度在界面校验，不能作为理解内容的证明，也不能作为分析追踪同意。
- 后端隔离验证：`/tmp/lmm-experience-verify` 中测试 controller、middleware、router、console_setting 的账号进度、公告顺序/重试/版本/账号隔离、预激活路由与配置校验通过。主工作区有其他并行开发的工具市场文件，曾因其未完成类型导致编译失败，因此未把隔离结果称为整个工作区后端通过。
- 前端：类型检查、生产构建通过；首页 19 项、Getting Started 11 项，以及指南、Dashboard 数据状态、布局、调试隔离、下一步与偏好保存定向测试通过；公告滚动与失败重试测试通过。
- Playwright 本地模拟数据验证：390px 手机和桌面视图，第一条到底后才能确认，确认后进入第二条。截图在 `output/playwright/`。这不是线上或真实多设备验收。
- 新代码定向 lint 通过；公告编辑器已有两处 `react(set-state-in-effect)` 报错，已在 HEAD 基线复现，未在本轮扩展修改。
- 尚未完成整体验收，也尚未部署。

## 下一批未完成工作

优先继续第 26 项来源分析的数据模型、许可与 URL 清洗、推广链接、业务节点幂等记录，再实现同批转化与留存报表。其余未完成的模型估算、导航术语、密钥分组联动、助手会话、赏金时间线等仍按上表推进，不能把当前改动视为 27 项全部完成。

## 第二批：来源分析基础（2026-09-19）

- 新增 `/operations/sources`，位于管理员“运营分析 → 用户来源”。推广链接支持生成、复制、二维码、重命名、归档和分页；`source_test=1` 测试访问不采集。
- 新的浏览器来源采集必须明确选择允许；尊重 Do Not Track。仅保留允许的来源参数、入口页面类别和来源网站主机名，排除 OAuth／付款回跳，不保存原始 URL、IP、User-Agent 或请求正文。
- 随机 HttpOnly Cookie 对应的服务端访客标识做 SHA-256 处理；已关联到一个账号的浏览器历史不可转移给另一个账号。匿名会话同一 nonce 重放不增加来源记录。
- 密码及通用 OAuth 的成功注册路径异步关联来源，500ms 超时，不影响注册成功。后续登录不补造注册来源。首次观察来源与注册来源分开保留，注册回看当前固定 30 天，并记录是否由历史记录归属。
- 推广链接的来源、活动、内容和目标在创建后不可重新分配，重命名／归档只改变显示属性；浏览器实际来源网站与推广标记独立保存。
- 注册来源报表按实际注册账号去重，显示识别覆盖的分子／分母。上线前未记录的历史账号显示“历史来源未记录”，不能根据后来访问倒推。
- 现金付款读取成功的充值订单、订阅订单和订阅周期付款事件，排除订阅充值镜像、余额支付、积分和赠送。退款读取独立退款记录，按币种分别汇总，不混入 API 消费或利润。缺少金额／币种依据的记录单独提示，不能按零填充。
- 原始来源明细保留 90 天，账号归属保留 365 天。清理任务属于服务器 runtime lifecycle，不在 migrate/verify 命令中运行。仍有保留期内访问记录的访客不能删除归属绑定，避免清理后造成跨账号归属。
- 查看汇总、管理推广链接、账号明细及导出权限独立；后两项默认不给普通管理员。目前仅开放汇总与链接接口，未发布账号明细／导出接口。
- 后端模型、控制器、权限定向测试通过，包含订阅镜像去重、续费、币种分离、赠送排除、未知支付证据、30 天回看、清理后的账号隔离。前端清洗测试通过。
- 前端隔离构建／类型检查通过；主工作区另有并行工具市场页面的语法错误，不将隔离结果描述为整个主工作区前端通过。

本批未完成：成功调用与留存、首次付费前来源、最近访问来源详情、自述来源、可调整回看、内容／活动筛选、完整漏斗和等待时间、来源修正审计、明细与导出、推广成本，以及完整的统计恢复和缺失覆盖度。推广链接测试目前只打开带排除标记的目标页，尚未实现专门的识别结果预览。现金报表是注册批次截至更新时间的观察结果，尚非统一后续观察期的比较报表。

- 来源页面已在独立本地 Persona Lab 的桌面及 390px 手机视口检查；样例数据不是线上运营结果。截图 `output/playwright/acquisition-desktop.png`、`acquisition-mobile.png`。手机截图中现有调试适配器仍提示未模拟的 ratio-notifications 接口，这不属于来源报表接口验证。

- 收尾检查：主工作区前端类型检查与生产构建已恢复通过（先前并行页面语法错误已消失），后端来源模型／控制器／权限测试及注册相关定向测试通过。来源汇总已显示判断依据并提示推广标记不等于实际投放平台；报表查询增加 3 秒限时和支付记录时间范围约束。仍未提交或部署。

## 第三批：已确认的调用、留存与账号来源详情

- 文本响应结算路径写入明确的 `acquisition_success_v1` 布尔标记：已写出响应、HTTP 2xx、有输出用量、非 Playground、正常流结束且无请求取消／记录的流错误。处罚扣费、渠道巡检、创建 Key 和 OAuth 授权都不作为成功调用。其他类型请求尚未纳入此口径。
- API 路径不增加统计数据库查询；标记随原有调用日志写入。独立 runtime 任务按分钟读取既有日志、按账号及 UTC 日汇总，不复制正文、IP、凭据或 Request ID。重复请求记录及重复扫描不会叠加活跃账号。
- 第 7 日留存以首次观察到合格成功调用的 UTC 日期为起点，只有第 7 天已完整处理的账号才进入分母。日志缺失区间所影响的账号单独计数，不能当成流失；缺失区间之外的完整批次仍可计算。处理进度、重建状态和未知值会显示在页面和 CSV 中。
- 汇总支持管理员重放恢复；账号自行更改许可不会重置全站扫描进度。数据源丢失且无法从既有日志恢复的部分不补零。正常提交延迟有重叠扫描保护，超出重叠范围的迟到记录需重建；写入失败的缺失标记在进程内汇集后由后台保存，不能将其声称为跨进程崩溃的零丢失保证。
- 扩展分析使用 v2 明确许可，旧版许可不会自动升级。账号级撤回偏好会阻止旧浏览器重新采集；撤回失败可见且可重试，Cookie 过期后仍可凭账号身份撤回。该偏好保留到修改或账号移除，独立于分析明细的保留期。
- 用户来源详情提供首次观察来源、注册归属及其证据、首次观察到的成功调用、最近保留的来源记录；渠道报表和用户详情可以相互进入。账号明细使用单独的 `acquisition.details` 权限，返回内容不含邮箱、用户名、访客 Cookie 或 API 凭据。
- 默认回看支持 1—90 天配置，只影响后续注册归属。旧记录保留当时窗口，报表显示实际记录过的窗口；汇总 CSV 导出保留币种、处理时间与未知值，并处理电子表格公式注入。未开放账号级批量导出。
- 后端定向测试覆盖成功判定、重复记录、观察中、缺失区间、许可升级／撤回、匿名路由拒绝、来源详情边界及回看不改历史。前端测试覆盖撤回失败重试、敏感参数过滤和汇总 CSV；类型检查与生产构建通过。SQLite 已执行验证；其他日志数据库有适配，但未在真实 PostgreSQL／MySQL／ClickHouse 实例上完成端到端验证。ClickHouse 元组查询参考：https://clickhouse.com/docs/reference/statements/in 。
- 手机端改为逐渠道的纵向指标，详细统计口径可以展开查看，桌面保留比较表。

仍未完成整份 27 项。来源分析还缺完整接入／付费漏斗和等待时间、首次付费前来源、用户自述、来源人工修正审计、账号明细导出、活动成本及完整推广链接识别预览；模型费用估算、导航术语、密钥分组联动、助手会话、赏金时间线等仍按前面的清单继续。没有提交或部署。

- 第三批页面复核使用本地样例数据，桌面和 390px 手机截图位于 `output/playwright/activity-desktop.png`、`activity-mobile.png`。渠道账号列表与用户来源详情已实际点击检查。用户编辑器原有 `react(incompatible-library)` lint 提示在 HEAD 基线可复现；新增来源组件 lint 通过。

## 第四批：模型详情费用估算

- 模型详情加入单次请求估算，默认输入 10,000 / 输出 2,000 Tokens。用户可选择当前模型与账号共同可用的分组，调整输入、输出和缓存命中 Tokens。
- 常规文本计费使用现有平台单位与分组倍率口径，缓存命中视为总输入的子集，不重复计算；倍率只应用一次。沿用价格页的平台余额／折算美元显示选项。按次计费使用明确配置的单次价格。
- 缺失价格、缺失分组倍率、非法 Tokens 和未知／动态表达式不猜算，显示无法估算并引导查看计费规则。有效零倍率保留为免费。
- 明确说明不含缓存写入、媒体与工具费用，估算不作为最终扣费承诺。动态阶梯计算及完整特殊计费模拟仍未完成，第 5 项尚不能标记全部完成。
- 七种语言通过项目翻译脚本写入并同步。计算测试覆盖缓存替换、分组倍率、零价、缺价、非法输入和按次计费；模型详情测试检查估算区域、输入默认值及标签关联。类型检查、定向 lint 与生产构建通过。本批未进行真实浏览器视觉检查，未提交或部署。

### 第四批续：文本阶梯表达式估算

- 费用估算现在支持 v1 的纯 Tokens 动态表达式，包括阶梯条件和受限数学函数。沿用已有受限表达式解释器，不执行 JavaScript。
- 与后端 `BuildTieredTokenParams` 对齐：`len` 始终是包含缓存的总输入；只有表达式引用 `cr` 时才从 `p` 中减去缓存。即使引用在另一个分支中也保持后端的静态变量识别规则。表达式结果除以 1,000,000 后仅乘一次分组倍率。
- 先检查所有分支，再计算选中分支；含请求头、参数、时间或未知函数／变量的表达式不出估算，避免条件暂时未命中就忽略实际计费所需信息。未知版本、非有限数值和负费用也不出估算。
- 前端 6 项计算测试、类型检查、定向 lint、生产构建及后端 billingexpr/service 阶梯定向回归通过。没有浏览器视觉复核，也没有提交或部署。
- 仍只估算一次纯文本请求，缓存写入、媒体、工具、实时渠道附加倍率以及请求上下文相关价格不在本次估算范围；这些限制继续在页面说明。第 5 项的完整特殊计费模拟和其余待办仍未全部完成。

## 第五批：并行推进导航、会话、赏金、Key 与排错

- 导航按使用 AI、开发者、生态、账户、其他服务分区，保留现有权限与旧分区配置兼容；公共价格入口统一为“模型与价格”，助手主名称统一为“AI 助手”。没有把仅重定向到助手的旧 Playground 路由冒充独立测试台。术语约定记录在 `docs/product-terminology.md`。
- Key 分组修改后使模型、价格、配额详情和助手相关查询失效重取，同时更新真实账号状态。创建默认名称使用已选择的设备／客户端，否则生成日期名称。修改或撤销后清空页面密钥缓存，晚完成的密钥读取响应也不能恢复已清除内容。现有再次读取密钥的后端能力尚未改为一次性，因此未加入虚假的“只显示一次”文案。
- 账号刷新增加账号及会话校验，防止切换账号时旧请求覆盖新用户状态。
- 赏金公开页、详情、接受弹窗及控制台卡片展示槽位、信誉、更新时间、验收与审核者、托管余额和 7 天申诉说明，奖励标明 API 余额。任务时间线使用真实接受、提交、拒绝、审核、申诉及到账时间，保留被申诉推翻的拒绝事件。结构化任务类型和独立开发开始时间仍无数据，不伪造。
- 模型详情区分账号分组资格与最近一小时真实调用结果；目录加载失败、权限拒绝及目录缺失分别处理。无维护／拥堵权威数据，因此不声称实时可用，也不把过滤后的缺失一律称为模型不存在。
- 日志详情增加费用与 Tokens 摘要、安全诊断复制、Request ID 复制及助手排错入口。429 仅在有明确错误代码时区分原因；没有关联账务依据时，不把单条日志金额当成整个请求最终扣费或退款事实。复制诊断使用结构化字段允许列表，无法解析的原始错误正文不复制。
- 本批仍在进行整合验收；助手持久请求去重、审核反馈与推广链接预览由并行子任务继续补齐。不能把这段记录当成全部 27 项已完成。

### 第五批收尾补充（合并前）

- 助手请求使用账号范围内的持久 turn ID，AI 回复和真人转接消息都支持重复成功请求重放；同文新 ID 不合并。消息裁剪后的重试明确报不可恢复，不自动生成新线程。收据随账号删除清理。该保证是历史记录幂等，不保证并发上游模型仅执行一次。
- 访问申请详情已接入实际账号状态区，显示提交与审核时间、申请内容、AI 建议及管理员备注；用户明确确认后可以修改或重新提交，界面说明旧审核备注会清除。
- 客户端预设补齐 Pi OAuth、AstrBot、OpenAI SDK、Anthropic SDK。Guide 与助手共享设备及客户端选择，Pi 路径不要求手动 Key，完成状态来自真实账号记录。SDK 示例仅本地输入凭据，未做第三方客户端实际付费请求验证，也不宣称尚未实现的 CLI 自动配置。
- 推广链接测试先展示只读识别预览，复用正式来源识别逻辑，测试打开使用排除标记；预览不写游客、访问、账号或统计配置。
- 可选来源问答支持保存、跳过、删除，保存 365 天；独立表与自动来源并列展示，不覆盖首次或注册归属，不更改邀请及财务记录。拒绝网址及明显凭据输入，账号级权限控制，过期／已删除账号数据由清理任务移除。
- Key 新建默认折叠分组及路由配置，保留选中分组摘要；配额与到期时间仍可选。Key 一次性显示与完整创建后配置闭环尚未全部完成。
- 用户于本轮要求今天所有 agent 的改动合入 main；这改变交付方式，不代表原 27 项已全部完成。后续来源完整漏斗、付费前归属、来源修正审计、账号级导出，以及部分实时模型状态与最终退款对账仍需继续。

- 活动成本按稳定推广链接、注册批次、注册后观察天数与币种分别记录。未提供支出为未知，显式零支出保留零；观察期未满或结算证据不全时不输出最终首次付费获客成本。不合并不同币种，不计算利润或 ROI。SQLite 定向模型／路由和前端金额测试通过，实际其他数据库及浏览器验收待后续。
- 本批后端全量 `go test ./...` 已通过；前端助手整文件 52 项连续两次通过，已修超时后的 React root 清理。账号刷新、来源自述删除失败重试、Key 分组缓存与迟到密钥响应、新手引导、来源清洗和预览等定向测试通过。最终合并其他分支后还需重新验证。

## 2026-09-19 main 整合验收

- 已按用户指令纳入当天主工作区、远端 main、脚本菜单分支及首页／游戏工作区的源码改动。视频、浏览器快照、临时发布包、node_modules 和设计会话文件保留本地并忽略。
- 保留首页动效与本批 OAuth/API Key 分流，按键合并七语言文案。新增游戏同时补齐普通和快速迁移，修复合并验证发现的测试数据与类型/lint问题。
- 合并后后端 `go test ./...` 全量通过；前端类型检查、生产构建通过；37 个变更测试文件已运行，模型状态一项异步等待问题修复后该文件 5 项复测通过，其他文件通过。游戏算法、store、WebMCP 另外 14 项通过，游戏控制器真实路由测试通过。此处不宣称全部前端测试套件均运行，也不代表生产部署验收。
- 本次仅合入本地 main，未推送、未部署。原始 27 项的剩余功能仍以前述未完成边界为准。

## 后续调整：移除动态分组利润倍率

- 用户明确要求移除。已删除按负载／渠道成本调节利润倍率的引擎、定时任务、配置模块、状态／设置 API、助手管理入口和前端设置页。
- 普通、按次、音频、任务、OAuth 模型目录、Tokens 阶梯预扣与结算不再叠加该倍率；固定分组倍率、用户折扣和 Tokens 阶梯表达式保留。
- 历史数据库配置不删除，但启动不加载、通用设置不展示且拒绝重新写入。旧任务中的退休倍率键被忽略。历史账单金额不改，界面仅保留旧价格调整的只读解释。
- 这项删除不等于取消其他体验优化；已完成的来源修正／导出、首付快照／漏斗、一次性 Key、模型状态代码保留。

- 删除范围同时覆盖 Rust 预览后端的动态倍率路由／注册与配置入口。Go `go test ./...` 全量通过；Web 类型检查、生产构建、受影响测试通过；Rust 离线 lib/bin 检查、退休配置定向测试与 route-completion gate 通过。仅保留旧配置拒用、旧任务倍率忽略及旧账单只读解释，不保留可重新启用的动态倍率功能。未部署。

### 本轮已保留的其他进展

- 来源漏斗按同一注册批次及相同观察天数，区分 OAuth/Key、未知证据和观察中；付费前归属保存规则及证据快照，迟到的更早付款另记冲突并降为未知。实付／付款时间缺失不以预期金额／订单创建时间替代。
- 管理员来源修正使用独立覆盖记录和追加审计，不改原始观察与财务；账号 CSV 单独要求 details/export 权限，默认无邮箱，限制 10,000 行并处理公式注入。
- 来源总览支持 UTC 今天及自定义区间，可识别访客按同意采集的浏览器标识去重；记录覆盖不足显式提示。
- 新手单个手动 Key 创建只返回一次完整 Key，后续单条／批量读取均由服务端阻止。旧 Key、自动 OAuth 及高级批量创建保持原有兼容行为；新建结果可复制、配置、只读检测与撤销。
- 模型运行状态来自当前可用路由及有到期时间的管理员声明，并区分账号权限。它不保证下一次上游请求成功。控制台新增最近请求概览；普通用量日志不自动标作成功响应。
- 上述功能已本地验证并补齐七语，尚未生产部署；整份原始清单的全流程验收和剩余细节仍未全部完成。
