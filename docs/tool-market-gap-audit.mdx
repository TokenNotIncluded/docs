---
title: "LMM 工具市场：需求与仓库实现差距"
description: "LMM API documentation: LMM 工具市场：需求与仓库实现差距"
---

核查日期：2026-09-19。基线：`4524697e8` 及核查时的本地工作树。

后续实施进度见 [工具市场实施记录](/docs/tool-market-implementation)。下文保留开始实现前的差距基线，不能当作实施后的最新功能清单。

结论：仓库已有账户、模型渠道市场、两个内置 MCP、原子余额预扣、财务流水、开源悬赏和基础 WebMCP；尚未找到需求书定义的通用 MCP 工具市场闭环。建设重点是新增市场业务模型、用户工具集、授权策略和调用交易流程，再接 Remote 与 Serverless 两种执行后端。

本次检查了 Go 主后端的路由、模型、服务、MCP 控制器，Web 页面与 WebMCP，以及 Rust MCP 入口。README 将 Go 标为默认生产后端，Rust 标为预览后端。本报告不代表线上已部署状态，也不是完整安全审计。未修改业务代码，未读取生产数据库或凭证。工作树原有其他改动未纳入本次修改。

## 本次对话确定的范围调整

用户明确取消提现机制，并要求“直接划转，留划转记录”。据此：

- 成功调用按确认价格和费率直接划转至作者账户和指定平台收款账户。
- 不建设待结算收益池、提现申请、外部收款账户、提现审核或打款流程。
- 保留调用记录与划转记录的关联、划出方和划入方、金额、费率快照、时间及防重复处理标识。
- 退款关联原交易，追加退回或冲正记录，不改写原划转事实。
- 用户调用前的金额冻结、执行结果待确认仍有必要；它们不属于作者收益提现或延迟结算。
- 原第 20 章应改为“作者收益与划转记录”；第 2、19、21、28、29 章中的提现和作者延迟结算条款相应删除或调整。Serverless 作者资源费用仍单独记录。

作者已经花掉收入后发生获批退款的资金承担规则，原需求中的待追回处理仍需明确；取消提现并不会自动消除这个情况。

## 证据索引

以下位置均为当前代码，可用于复查；“缺失”表示在核查的业务模型、路由、执行链路和页面中未找到实现，不是仅凭名称检索就判断不存在。

| 编号 | 代码位置 | 已确认的内容 |
| --- | --- | --- |
| E01 | [README](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/README.md#L44) | Go 为默认后端，Rust 为预览后端。 |
| E02 | [悬赏 MCP 路由](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/router/open_source_bounty_mcp_router.go#L9)、[处理器](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/controller/open_source_bounty_mcp_server.go#L699) | `/mcp` 指向固定注册的悬赏工具；采用无状态 HTTP handler，支持个人令牌及特定 OAuth scope。 |
| E03 | [绘图 MCP](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/controller/drawing_mcp_server.go#L191) | 内置绘图能力与生成工具；确认后调用既有绘图 relay。 |
| E04 | [OAuth 配置与客户端](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/service/oauth_server.go#L24)、[授权模型](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/oauth_server.go#L38) | Pi、DSH 固定客户端；模型、账户与两个内置 MCP scope；令牌家族、有效期、撤销及摘要存储。配置默认不开启。 |
| E05 | [登录 OAuth 绑定](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/user_oauth_binding.go#L10) | 保存第三方登录身份关系，不是用于调用第三方资源的用户授权凭证库。 |
| E06 | [远程 MCP 搜索适配](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/controller/assistant_search.go#L391) | 已有 MCP client、连接、列举 Tool、选取搜索工具并执行；按目标 origin 附加独立配置令牌，识别 MCP `IsError`。 |
| E07 | [WebMCP](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/web/src/features/webmcp/index.ts#L76) | 站点信息、导航、模型价格、账户状态四种辅助能力。 |
| E08 | [渠道市场模型](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/public_relay_contribution.go#L46) | 渠道提交审核、举报、评分、打赏及用户路由偏好；不是 MCP 服务与 Tool 目录。 |
| E09 | [钱包预扣](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/quota_reserve.go#L208)、[钱包资金来源](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/service/funding_source.go#L41) | 条件更新实现原子余额预扣；钱包退款仍是增加余额的非幂等操作，代码明确注明不能直接重试。 |
| E10 | [计费会话](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/service/billing_session.go#L25)、[订阅结算](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/subscription_billing.go) | 普通计费会话有进程内状态，订阅结算已有持久化事务路径；不能把所有计费都视为仅内存，也不能把订阅能力当成通用工具账务。 |
| E11 | [财务流水](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/finance_ledger.go#L34)、[支付退款](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/payment_refund.go#L45) | 追加式流水、冲正关联、幂等键；支付退款针对充值/订阅订单，不能直接当作工具消费退款。 |
| E12 | [悬赏数据模型](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/open_source_bounty.go#L67)、[争议](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/open_source_bounty_dispute.go#L283) | 草稿、资金托管、手续费、验收付款、退回未使用预算、流水、评分和争议；争议处理排除直接当事管理员。 |
| E13 | [渠道打赏划转](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/public_relay_contribution.go#L649) | 名称虽为 Withdraw，实际向站内余额入账，不是外部提现；仍是手动领取打赏流程。 |
| E14 | [异步任务](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/task.go#L36)、[超时清理](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/service/task_polling.go#L47) | 有任务状态、用户关联、进度、结果、退款标记；现有超时清理会写失败状态，不是需求中的通用结果待确认流程。 |
| E15 | [HTTP 客户端](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/service/http_client.go#L150)、[SSRF 防护](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/common/ssrf_protection.go) | 有 URL、重定向和拨号防护组件，但受全局开关及内网策略影响；普通 relay HTTP client 与受保护 client 用途不同。 |
| E16 | [角色鉴权](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/middleware/auth.go#L117)、[管理员审计](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/middleware/audit.go#L44)、[操作日志](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-go/model/log.go#L264) | 用户、管理员、超级管理员边界及操作审计基础。 |
| E17 | [Rust MCP](https://github.com/TokenNotIncluded/api.lmm.best/blob/main/apps/api-rust/src/routes/mcp.rs#L99) | 当前入口鉴权和初始化后返回工具执行未配置；不能作为第二套已完成的市场执行后端。 |

## 31 章逐项对照

“部分基础”只表示相邻业务有可复用实现，不表示这一章已经验收。

| 需求章 | 判断 | 当前实现与主要差距 |
| --- | --- | --- |
| 1 产品定位 | 主体缺失 | 有渠道市场和内置 MCP，但没有 MCP 服务、独立 Tool、执行类型、来源标识组成的市场目录。E02、E03、E08。 |
| 2 角色权限 | 部分基础 | 用户/管理员/超级管理员鉴权可用；缺作者资源归属、市场操作权限、审核与凭证访问隔离策略。不新增提现角色流程。E16。 |
| 3 市场与搜索 | 缺失 | 现有模型价格和渠道市场不是工具市场；缺工具搜索、免费分类、执行类型过滤、私有可见性、收藏和使用历史。E08。 |
| 4 详情页 | 缺失 | 缺服务详情、逐 Tool 定义/价格/权限、数据接收方、统计样本与时间范围、运行限制和许可证信息。 |
| 5 元 MCP | 部分基础 | `/mcp` 当前专用于悬赏，绘图另有入口；缺账户工具目录、动态可用工具清单和统一路由。OAuth 也不是所列全部客户端的通用接入。E02—E04。 |
| 6 收藏与加载 | 缺失 | 渠道路由偏好不能代替客户端工具集；缺服务/Tool 选择加载、收藏分离、清单刷新和卸载后阻止新调用。E08。 |
| 7 授权与预算 | 部分基础 | 有 scope、令牌额度及撤销；缺逐客户端/Tool/操作授权、单次价格上限、账户/客户端/Tool 多层预算和并发占用。E04、E09。 |
| 8 OAuth 与 Secret | 部分基础 | LMM OAuth 令牌摘要和登录绑定已有；缺工具第三方授权流程、作者/用户/平台凭证分层、按调用授权使用与安全代认证。E04—E06。 |
| 9 发布流程 | 缺失 | 悬赏草稿与渠道投稿可参考，但没有 MCP 发布草稿、文件/远端配置校验、指定用户访问、预览和审核流程。E08、E12。 |
| 10 Remote 接入 | 部分基础 | 搜索功能已有远端 MCP 连接/列工具/调用代码；缺作者接入、兼容性检查、错误分类、配置版本审核和数据接收方变更确认。E06、E15。 |
| 11 Serverless 运行 | 缺失 | 未找到产品级 WASM 上传、接口校验、执行器、能力授予、网络/内存/时间限制和调用级清理；依赖锁文件中的 wasmtime 名称不算实现。 |
| 12 资源预算费用 | 缺失 | 缺作者资源账单、工具/作者预算、补贴账、平台并发/排队限制及仅暂停 Serverless 的开关。 |
| 13 Tool 独立管理 | 缺失 | 内置工具名固定；缺市场级稳定标识、逐 Tool 价格与权限、远端同步差异及新增 Tool 不自动加载的规则。E02、E03、E06。 |
| 14 WebMCP/Agent | 部分基础 | 已有四个 WebMCP 辅助工具；悬赏 MCP 有敏感操作确认。缺市场草稿操作、字段检查、同步预览、发布审核及对应后端授权。E02、E07、E12。 |
| 15 版本与上下架 | 缺失 | 渠道/悬赏状态不能替代不可变已审核版本、待审核更新、版本差异、价格生效、权限再确认及不同执行类型的回滚边界。 |
| 16 定价与免费额度 | 缺失 | 模型报价不是 Tool 价格；缺 Tool 价格版本、组合免费规则、并发预留免费额度、成功后消耗和免费转付费授权。E03、E10。 |
| 17 调用与成功判定 | 部分基础 | 有模型预扣/结算，远端搜索识别 MCP 错误；缺通用调用记录、授权/价格快照、结果契约验证，以及独立执行状态和划转状态。E06、E09、E10。 |
| 18 重复/超时/长任务 | 部分基础 | 有订阅请求幂等及媒体任务持久化；缺工具业务调用 ID 与内容绑定、结果未知禁止自动重试、待确认期限、取消语义和重复领取结果。E10、E14。 |
| 19 手续费与分成 | 部分基础 | 悬赏已保存费率及手续费并记账；缺 Tool 成功调用的费率优先级、收款账户快照、直接划转与调用幂等的一致事务。E11、E12。 |
| 20 作者收益 | 按新范围简化，仍需实现 | 不做提现/待结算池。新增直接划转流水、Tool 维度收益查询、退款冲正与导出；渠道打赏手动领取不是该流程。E11—E13。 |
| 21 退款与争议 | 部分基础 | 有充值/订阅退款和悬赏争议；缺针对调用的申请、累计部分退款上限、作者/平台原划转冲正及余额不足后的追回记录。E11、E12。 |
| 22 调用统计 | 部分基础 | 有模型消费日志与任务时间；缺 Tool、版本、客户端、免费/付费、待确认、作者自测分离及样本口径。E14、E16。 |
| 23 隐私与日志 | 部分基础 | 有隐藏私有字段及账户过滤的实现；缺市场数据传递规则、作者日志视图、限时调试授权、结果保留/删除及脱敏流程。不能据此断言既有所有日志已泄露或已经合规。E04、E14、E16。 |
| 24 健康与保护 | 部分基础 | 有限流、网络防护和任务超时；缺 MCP 无副作用探测、个人授权失效与全局离线区分、作者预算和工具级故障隔离。E02、E14、E15。 |
| 25 评价举报 | 部分基础 | 渠道有评价举报，悬赏有双向评分；缺绑定真实 Tool 使用的评价、作者回复、市场认证范围和工具描述/返回内容的诱导行为处理。E08、E12。 |
| 26 来源与悬赏 | 部分基础较多 | 通用开源悬赏已有资金托管、提交、验收和争议；缺工具交付关联、许可证/公开范围结构化信息、发布者与后续收益归属及市场上架审核。E12。 |
| 27 推广 | 缺失 | 未找到工具付费推广计划、预算消耗、暂停联动、广告标识及曝光到真实调用的独立统计。 |
| 28 管理控制台 | 部分基础 | 管理员/超级管理员和审计可复用；缺工具审核、市场费率、执行类型停用、资源总限额及新交易暂停。删除提现和延迟结算设置。E16。 |
| 29 通知与审计 | 部分基础 | 悬赏通知、价格类通知与管理员审计存在；缺市场完整事件、权限/价格/预算/划转/退款通知及通知偏好。E12、E16。 |
| 30 恢复与故障 | 部分基础 | 内置 MCP handler 无状态，OAuth/任务/悬赏使用数据库；缺市场调用的执行与资金恢复流程，以及共享状态故障时统一拒绝放行。E02、E04、E09、E10、E14。 |
| 31 兼容与扩展 | 部分基础 | 已有 MCP SDK 和内置入口；缺市场客户端能力表、Serverless 接口约定、工具兼容要求及执行后端接入约定。Rust 入口尚未实现业务工具执行。E02—E04、E17。 |

## 最需要避免的六处直接套用

### 1. 把现有 `/mcp` 改名就称为元 MCP

当前 handler 共享一个固定注册的悬赏 server，并不是按账户和客户端加载工具清单。需要真正加入工具集、访问检查、稳定 Tool ID 和动态清单。现有悬赏客户端已经使用此路径，后续接入市场时还需明确兼容方式，不能无意改变旧入口的工具和授权语义。

### 2. 把站点 OAuth scope 当作 Tool 消费授权

当前注册的是 Pi 与 DSH，scope 对应模型和内置 MCP；授权模型没有 Tool 价格上限、次数和累计预算。现有登录绑定也不是外部资源凭证。应复用 OAuth 核心验证，再补市场授权策略和独立第三方连接记录。

### 3. 只在调用结束后分别改三次余额

用户扣费、作者入账、平台入账、划转记录和调用结算标记必须作为同一笔交易处理，或具备可恢复且不会重复执行的等价机制。否则中途退出会产生少记、重复入账或查不到对应调用的问题。

已有钱包原子预扣可以复用，财务流水有幂等键和冲正模式；但普通钱包退款路径明确是非幂等增加余额，`BillingSession` 的部分完成标记只在内存中。不能仅靠内存锁实现跨实例/重启后的防重。订阅持久化结算与媒体任务退款提供参考，而非零基础重做所有账务。

特别复查：`shouldTrust` 对当前钱包和订阅均返回 false，不能因旧注释提到“信任旁路”就误报当前钱包会跳过预扣。

### 4. 让工具调用直接继承模型任务的超时处理

现有媒体任务超时清理会写 `FAILURE` 并进入退款处理。对可能已在远端发送消息、写入或支付的 MCP，这不足以证明执行失败。需要区分“未执行”“明确失败”“结果未知”，未知状态不能自动重放副作用操作；到期限释放用户冻结金额，也不能把外部操作标成已撤销。

### 5. 复用可关闭的抓取防护作为公开 Remote 接入的唯一边界

已有 SSRF 防护可复用，但其行为受全局开关和允许内网等配置影响。公开作者接入需要专门、明确的出站策略，覆盖服务地址、OAuth 发现、重定向、资源访问和实际拨号。不能把管理员可配置上游地址的信任模型原样交给普通作者。

### 6. 把 Serverless 当成上传入口加一个执行命令

仓库没有可供市场直接复用的 WASM 运行闭环。文件兼容性、宿主能力、调用隔离、资源上限、作者资源计费、取消清理和全局暂停都需建设。可以在同一套市场模型预留 Serverless 类型，但在运行边界完成前不开放用户代码执行。

## 建议的改造顺序

1. **市场对象与权限**：服务、Tool、已审核版本、可见范围、收藏、客户端工具集、授权策略。先把这些边界固定，避免把服务和 Tool 混成一张配置表。
2. **调用与直接划转**：持久化调用记录、价格/费率快照、免费额度和预算预留、冻结、成功划转、失败释放、未知结果处理、幂等和退款。复用现有账户余额，不建设提现系统。
3. **Remote 完整路径**：作者草稿 → 检查/同步 → 审核发布 → 用户授权加载 → 元 MCP 调用 → 划转/退款 → 查询记录。旧内置 MCP 保留兼容。
4. **Serverless 完整路径**：沿用同一目录、授权和调用账务，补运行时、能力约束、资源费用和限额。失败调用的资源成本不能转成用户工具费。
5. **市场运营完善**：统计、评价举报、推广、通知和悬赏成果接入。基础审计、隐私保护和紧急停用必须随前面的功能同时落地，不能留到最后。

这是依赖顺序，不是擅自删除需求书中的 Serverless 或运营功能。当前最适合先形成可验收闭环的是 Remote MCP；Serverless 可以在共享对象和接口确定后单独建设。

## 本次验证

本地定向执行以下 7 个现有测试，三个包均通过：

```text
TestDrawingMCPAuthenticationDiscoveryAndConfirmation
TestDrawingMCPConfirmationRejectsForgeryWrongPayloadReplayAndDoubleSubmit
TestOpenSourceBountyMCPAuthenticationToolsAndPublishConfirmation
TestOAuthHTTPDiscoveryAndDisabled
TestOAuthHTTPResourceBillingIsolationAndRevocation
TestApplyWalletQuotaDeltaEnforcesSymmetricSafeBounds
TestUpdateWalletQuotaByDeltaKeepsCallerPredicates
```

执行命令：

```sh
cd apps/api-go
go test ./controller ./router ./model -run '^(TestDrawingMCPAuthenticationDiscoveryAndConfirmation|TestDrawingMCPConfirmationRejectsForgeryWrongPayloadReplayAndDoubleSubmit|TestOpenSourceBountyMCPAuthenticationToolsAndPublishConfirmation|TestOAuthHTTPDiscoveryAndDisabled|TestOAuthHTTPResourceBillingIsolationAndRevocation|TestApplyWalletQuotaDeltaEnforcesSymmetricSafeBounds|TestUpdateWalletQuotaByDeltaKeepsCallerPredicates)$' -count=1
```

这些测试证明相应现有基础功能在本地测试环境通过，不证明工具市场已经实现，也不证明真实客户端互通、生产启用、第三方操作或 WASM 沙箱安全。未运行全量测试及线上端到端验证。
